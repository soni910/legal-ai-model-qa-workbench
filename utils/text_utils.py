"""Text utilities for normalization and comparisons."""


def normalize_text(value: str) -> str:
    """Simple normalization placeholder for skeleton stage."""
    return " ".join(value.strip().lower().split())
